{% trans_default_domain 'admin' %}

{% extends 'back/layout.html.twig' %}

{% block title %}{{ 'admin.user.create.title'|trans }}{% endblock %}

{% block content %}
    <section class="section">
        <div class="row">
            <div class="col-12">
                <a href="{{ path('app_admin_user_index') }}">{{ "admin.back_to_list"|trans }}</a>
                <div class="card">
                    <div class="card-header">
                        <h1 class="card-title">{{ 'admin.user.create.title'|trans }}</h1>
                    </div>
                    <div class="card-content">
                        <div class="card-body">
                            {# start card content #}
                            {{ include('back/user/_form.html.twig') }}
                            {# end card content #}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
