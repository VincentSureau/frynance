{% trans_default_domain 'admin' %}

{% extends 'back/layout.html.twig' %}

{% block title %}{{ 'admin.quote.index.title'|trans }}{% endblock %}

{% block content %}
<section class="section">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h1 class="card-title">{{ 'admin.quote.index.title'|trans }}</h1>
                </div>
                <div class="card-content">
                    <div class="card-body">
                        <a class="btn btn-primary" href="{{ path('app_admin_quote_new') }}">{{ 'admin.quote.create_new'|trans }}</a>
                        <div class="table-responsive">
                            <table class="table table-lg">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>{{ 'admin.quote.title'|trans }}</th>
                                        <th>{{ 'admin.quote.totalRecipes'|trans }}</th>
                                        <th>{{ 'admin.quote.totalLabour'|trans }}</th>
                                        <th>{{ 'admin.quote.totalPrice'|trans }}</th>
                                        <th>{{ 'admin.quote.sellPrice'|trans }}</th>
                                        <th>{{ 'admin.quote.actions'|trans }}</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for quote in quotes %}
                                        <tr>
                                            <td class="text-bold-500">{{ quote.id }}</td>
                                            <td>{{ quote.title }}</td>
                                            <td>{{ quote.totalRecipe|format_currency('EUR') }}</td>
                                            <td>{{ quote.totalLabour|format_currency('EUR') }}</td>
                                            <td>{{ quote.totalPrice|format_currency('EUR') }}</td>
                                            <td>{{ quote.sellPrice|format_currency('EUR') }}</td>
                                            <td class="text-bold-500">
                                                <a class="btn btn-primary btn-sm" href="{{ path('app_admin_quote_edit', {'id': quote.id}) }}"><i class="bi bi-pencil"></i> {{ "admin.edit"|trans }}</a>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td class="text-bold-500" colspan="4">{{ "admin.no_reccords"|trans }}</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>

</section>
{% endblock %}